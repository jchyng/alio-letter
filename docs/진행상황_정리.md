# 진행상황 정리

## 2026-03-14

### pipeline/scraper.py + pipeline/tests/test_scraper.py 생성 (TDD RED 단계)

**작업 내용**
- `pipeline/scraper.py`: `_fetch_page()` / `fetch_posting_list()` 함수 시그니처 정의 (빈 껍데기)
- `pipeline/tests/test_scraper.py`: HTML fixture + 3개 테스트 작성
  - `test_returns_all_postings_when_last_no_is_zero` — last_no=0 이면 전체 반환
  - `test_posting_has_required_fields` — 필수 필드(alio_id, title, org_name, posting_url, posted_date, deadline) 확인
  - `test_filters_by_last_no` — last_no=10001 이면 alio_id=10002 만 반환

**현재 상태**: 3 FAILED (RED) — 구현 대기 중

**다음 단계**: `_fetch_page` / `fetch_posting_list` 구현 → GREEN
